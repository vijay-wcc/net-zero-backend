version: '3.8'
services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5432:5432"
    volumes:
      - net_zero_db:/var/lib/postgresql/data

volumes:
  net_zero_db:
